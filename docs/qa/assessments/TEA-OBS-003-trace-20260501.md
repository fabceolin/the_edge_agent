# Requirements Traceability Matrix

## Epic: TEA-OBS-003 — LLM Payload Trace Capture

**Date:** 2026-05-01
**Reviewer:** Quinn (Test Architect)
**Mode:** YOLO (non-interactive, epic-level rollup)
**Story status at trace:** Draft (all 3 child stories Draft; no implementation merged)
**Planning references:**
- [TEA-OBS-003-test-design-20260501.md](TEA-OBS-003-test-design-20260501.md) (epic-level rollup)
- [TEA-OBS-003.1-trace-20260501.md](TEA-OBS-003.1-trace-20260501.md) (child story trace)
- [TEA-OBS-003-risk-20260501.md](TEA-OBS-003-risk-20260501.md)
- [TEA-OBS-003-nfr-20260501.md](TEA-OBS-003-nfr-20260501.md)

---

## Important Status Caveat

**This epic is Draft.** No production code or tests have been merged for TEA-OBS-003.* — verified by:
- `grep` for `auto_trace_llm_payloads`, `trace_payload_retention`, `trace_payload_async`, `trace_payload_compress`, `LlmPayloadFileExporter`, `AsyncFileExporter` across `python/` returns **zero hits**.
- `python/tests/` has no `test_llm_payload_capture.py`, no payload-related test file.
- All three child stories (003.1, 003.2, 003.3) are in `Draft` status.

The Coverage column below therefore reflects **planned-test coverage from the epic-level test design** (78 scenarios), not coverage by tests merged in `main`. This trace must be re-run after each child story's implementation lands so that planned scenarios can be confirmed against actual test files.

---

## Coverage Summary (by Functional + NFR ACs)

The epic itself does not enumerate ACs directly; the contractual ACs live in the three child stories plus the 12 NFR-derived ACs (NFR-AC-1..12) added by the NFR review. Coverage is measured against this consolidated AC set.

| Source | AC count | Full (planned) | Partial (planned) | None |
|---|---|---|---|---|
| Story 003.1 functional ACs | 18 | 17 | 1 | 0 |
| Story 003.2 functional ACs | 17 | 17 | 0 | 0 |
| Story 003.3 functional ACs | 19 | 19 | 0 | 0 |
| NFR-AC additions (NFR-AC-1..12) | 12 | 12 | 0 | 0 |
| **Epic-level Definition-of-Done items** | 6 | 5 | 1 | 0 |
| **Total** | **72** | **70 (97%)** | **2 (3%)** | **0 (0%)** |

| Coverage Level | Definition |
|---|---|
| `full (planned)` | ≥1 planned scenario covers the AC; Given-When-Then unambiguous |
| `partial (planned)` | Planned scenarios cover the AC but a documented sub-aspect is unverified |
| `none` | No planned or merged test addresses the AC |

---

## Traceability Matrix — Per-Story Rollup

### Story TEA-OBS-003.1 (Core capture + separate exporter + flag)

The detailed AC-by-AC traceability lives in [`TEA-OBS-003.1-trace-20260501.md`](TEA-OBS-003.1-trace-20260501.md). Summary:

| AC group | ACs | Coverage | Mitigated risks |
|---|---|---|---|
| Settings & flag (AC-1..3) | YAML setting; CLI `--trace-llm-payloads`; CLI `--trace-llm-payloads-for` | FULL (planned) | SEC-001, SEC-002, TECH-004 |
| Captured fields (AC-4) | 7-field schema | FULL (planned) | — |
| Binary placeholder (AC-5) | bytes → `{"type":"binary_omitted","size_bytes":N}` | FULL (planned) | DATA-001 |
| Default-off no-leak (AC-6, AC-13) | Filesystem snapshot + fuzz matrix | FULL (planned) | SEC-001, DATA-001 |
| Separate exporter file (AC-7..9) | `<basename>.llm.jsonl` + fallback path + INFO log | FULL (planned) | OPS-003 |
| Per-node glob filtering (AC-10..12) | Match logic; dynamic_parallel; non-match no-write | FULL (planned) | SEC-002 |
| Compatibility (AC-14, AC-15) | Coexists with `auto_trace`/`trace_exporter`/`trace_file`/Opik; works with TEA-DX-001.2 | FULL (planned) | TECH-002 |
| Quality (AC-16) | Meta-AC | FULL (planned, transitive) | — |
| Documentation (AC-17, AC-18) | Observability page + PII warning | FULL (AC-17) / **PARTIAL (AC-18)** | BUS-001, SEC-001 |

**Partial:** AC-18 — DOC-002 verifies PII-warning text exists but no automated check guarantees the warning stays at the top of the section if the doc is later refactored. Recommendation #1 below addresses this.

---

### Story TEA-OBS-003.2 (Retention policy & cleanup)

#### AC-1 — YAML setting `trace_payload_retention_days` (integer, default unset)
**Coverage: FULL (planned) — unit**
- **Unit:** `003.2-UNIT-001` (P0)
  - **Given:** YAML with `trace_payload_retention_days: 30` (or unset, or `0`, or negative)
  - **When:** Settings parser runs
  - **Then:** Positive int accepted; `0` and negative rejected with actionable error

#### AC-2 — Warning when capture-on + retention unset
**Coverage: FULL (planned) — unit**
- **Unit:** `003.2-UNIT-002` (P0)
  - **Given:** `auto_trace_llm_payloads=true`, `trace_payload_retention_days` unset, dev mode
  - **When:** Engine init runs
  - **Then:** Exactly one WARNING log line containing the AC-2 wording verbatim

#### AC-3 — No warning when both set
**Coverage: FULL (planned) — unit**
- **Unit:** `003.2-UNIT-003` (P1)
  - **Given:** Both settings present
  - **When:** Engine init runs
  - **Then:** Zero warning log lines about retention

#### AC-4 — Setting does NOT trigger automatic deletion
**Coverage: FULL (planned) — unit**
- **Unit:** `003.2-UNIT-005` (P1)
  - **Given:** Capture on, retention 30 days, fixtures with files older than 30 days present at engine init
  - **When:** Engine init runs (without invoking `tea trace cleanup`)
  - **Then:** No file is deleted; only inspection / logging occurs

#### AC-5 — `tea trace cleanup` subcommand exists with documented args
**Coverage: FULL (planned) — unit + integration**
- **Unit:** `003.2-UNIT-021` (P2)
  - **Given:** `tea trace cleanup --help`
  - **When:** Help text printed
  - **Then:** All flags (`--older-than`, `--dry-run`, `--pattern`, `--recursive`) documented
- **Integration:** `003.2-INT-001` (P0) covers end-to-end CLI invocation

#### AC-6 — Default `<dir>` is the `trace_file` directory or CWD
**Coverage: FULL (planned) — unit**
- **Unit:** `003.2-UNIT-010` (P0) covers path resolution logic over (config file, no config) inputs

#### AC-7 — `--older-than <days>` overrides YAML; error if neither
**Coverage: FULL (planned) — unit**
- **Unit:** `003.2-UNIT-001` (P0) — boundary cases
- **Unit:** `003.2-UNIT-014` (P0) — `--older-than 0` purge mode
- **Unit:** Error path when neither is set (planned within `003.2-UNIT-021` help/usage envelope)

#### AC-8 — Default `--pattern` matches `*.llm.jsonl` and `*.llm.jsonl.gz`
**Coverage: FULL (planned) — unit (gate-blocker for OPS-002)**
- **Unit:** `003.2-UNIT-011` (P0)
  - **Given:** Directory with `prod.jsonl`, `prod.llm.jsonl`, `prod.llm.jsonl.gz`, `arbitrary.txt`
  - **When:** Default-pattern matcher evaluated
  - **Then:** Only the two `.llm.` files match; spans-only `prod.jsonl` and unrelated `arbitrary.txt` excluded
- **Unit:** `003.2-UNIT-016` (P1) — anti-collision: spans-only file never matches even with `--pattern '*.jsonl'`

#### AC-9 — `--dry-run` lists candidates without deleting
**Coverage: FULL (planned) — unit + integration**
- **Unit:** `003.2-UNIT-013` (P0)
  - **Given:** Directory with 5 candidate files
  - **When:** `--dry-run` invoked
  - **Then:** Zero `os.remove` calls; output lists each file with size + mtime
- **Integration:** `003.2-INT-002` (P1) confirms via real filesystem

#### AC-10 — Without `--dry-run`, files older than TTL deleted; summary line printed
**Coverage: FULL (planned) — unit + integration**
- **Unit:** `003.2-UNIT-010` (P0) — pure logic over (paths, mtimes, now, older_than_days)
- **Integration:** `003.2-INT-001` (P0)
  - **Given:** Fixture with files at 1/7/30/60/90 days old + `--older-than 30`
  - **When:** Cleanup runs
  - **Then:** Files at 60d and 90d deleted; 30d retained; summary `Deleted N files, freed X MB` printed

#### AC-11 — Non-recursive default; `--recursive` for subdirs
**Coverage: FULL (planned) — unit**
- **Unit:** `003.2-UNIT-018` (P2)
  - **Given:** Nested directory tree with candidates at depth 0/1/2
  - **When:** Default vs `--recursive` invoked
  - **Then:** Default deletes only depth 0 matches; `--recursive` traverses

#### AC-12 — Exit 0 on success (incl. zero matches)
**Coverage: FULL (planned) — unit**
- **Unit:** `003.2-UNIT-020` (P1)
  - **Given:** Empty directory or no-match pattern
  - **When:** Cleanup runs
  - **Then:** Exit 0, summary `Deleted 0 files, freed 0 MB`

#### AC-13 — Exit 1 on deletion error; continue + report at end
**Coverage: FULL (planned) — integration**
- **Integration:** `003.2-INT-003` (P1)
  - **Given:** Mixed permissions: 3 deletable + 1 read-only file
  - **When:** Cleanup runs
  - **Then:** Exit 1; 3 files deleted; failure reported; remaining deletions completed before exit

#### AC-14 — `--help` includes example one-liner
**Coverage: FULL (planned) — unit**
- **Unit:** `003.2-UNIT-021` (P2)
  - **Given:** `tea trace cleanup --help`
  - **When:** Output captured
  - **Then:** Contains string `tea trace cleanup ./traces --older-than 30 --dry-run` verbatim

#### AC-15 — Docs include cron + systemd + TTL guidance
**Coverage: FULL (planned) — doc smoke**
- **Unit (doc):** `003.2-UNIT-030` (P3)
  - **Given:** `docs/python/observability.md`
  - **When:** Substring search runs
  - **Then:** Contains the cron one-liner, systemd timer block, and TTL guidance paragraph

#### AC-16 — Test coverage breakdown enumerated
**Coverage: FULL (planned, transitive)** — satisfied by union of AC-1..15 scenarios. Concrete mapping:
- warning emitted when retention unset → `003.2-UNIT-002`
- warning suppressed when set → `003.2-UNIT-003`
- cleanup respects `--older-than` → `003.2-INT-001`
- `--dry-run` deletes nothing → `003.2-UNIT-013`, `003.2-INT-002`
- `--pattern` filters correctly → `003.2-UNIT-011`, `003.2-UNIT-012`
- `--recursive` traverses → `003.2-UNIT-018`
- permission errors don't abort → `003.2-INT-003`

#### AC-17 — Test fixture creates files with controlled mtimes
**Coverage: FULL (planned) — fixture-level**
- **Fixture:** `aged_files_factory` (documented in test design §7.1) — uses `os.utime` to control mtimes at 1/7/30/60/90 days; consumed by `003.2-UNIT-010`, `003.2-INT-001/002`, `003.2-E2E-001/002`

#### Cross-story: AC-13/AC-15-style cleanup flows
**Coverage: FULL (planned) — E2E**
- **E2E:** `003.2-E2E-001` (P0)
  - **Given:** Real workflow with capture on → file written → `os.utime` ages it
  - **When:** `tea trace cleanup --older-than 1` invoked
  - **Then:** Payload file removed; spans file untouched
- **E2E:** `003.2-E2E-002` (P1) — multi-run accumulation; cleanup retires only the older file

---

### Story TEA-OBS-003.3 (Async exporter + optional gzip)

#### AC-1 — `AsyncFileExporter(path, queue_size=1000, flush_interval_s=5)` class exists
**Coverage: FULL (planned) — unit**
- **Unit:** `003.3-UNIT-001` (P1)
  - **Given:** `AsyncFileExporter(path, queue_size=10, flush_interval_s=0.1)` instantiated
  - **When:** Class introspected
  - **Then:** Constructor signature matches; `path`, `queue_size`, `flush_interval_s` exposed

#### AC-2 — Implements `TraceExporter` Protocol
**Coverage: FULL (planned) — unit**
- **Unit:** `003.3-UNIT-001` (P1) — `isinstance(AsyncFileExporter(...), TraceExporter)` or duck-typed `export(span)` callable

#### AC-3 — `export()` returns in <1ms wall-clock
**Coverage: FULL (planned) — unit**
- **Unit:** `003.3-UNIT-001` (P1)
  - **Given:** `AsyncFileExporter` with worker thread alive but slow disk
  - **When:** `export(span)` called and timed
  - **Then:** Wall-clock < 1 ms; span enqueued (queue size +1)

#### AC-4 — Background thread batches up to 50 spans per flush, or `flush_interval_s` boundary
**Coverage: FULL (planned) — unit**
- **Unit:** `003.3-UNIT-002` (P1)
  - **Given:** `AsyncFileExporter` with `flush_interval_s=10` and 50 spans queued
  - **When:** Worker runs
  - **Then:** Batch boundary fires at 50 spans (not at 10s); single `open/write/close` cycle observed
  - **AND:** With `flush_interval_s=0.1` and 5 spans queued, flush fires on the 0.1s timer

#### AC-5 — Shutdown drain with timeout; warn on dropped spans
**Coverage: FULL (planned) — integration**
- **Integration:** `003.3-INT-010` (P0)
  - **Given:** Async exporter with pending writes; `engine.close()` called
  - **When:** Shutdown timeout (default 30s) elapses
  - **Then:** Queue length 0 within timeout; no warning emitted on happy path
- **Unit:** `003.3-UNIT-010` (P1)
  - **Given:** Slow disk causing partial drain at timeout
  - **When:** Shutdown completes with N undrained
  - **Then:** WARN log lists the count; spans best-effort flushed

#### AC-6 — Queue overflow policy (`drop_newest` default; `block` opt-in)
**Coverage: FULL (planned) — unit**
- **Unit:** `003.3-UNIT-020` (P1)
  - **Given:** `overflow_policy=drop_newest`, queue full
  - **When:** Additional `export()` calls fire
  - **Then:** New spans dropped; counter incremented; WARN throttled to 1/min
- **Unit:** `003.3-UNIT-021` (P2)
  - **Given:** `overflow_policy=block`, queue full
  - **When:** `export()` called
  - **Then:** Blocks until space; verified via `threading.Event` rendezvous

#### AC-7 — `trace_payload_async: true` setting wires AsyncFileExporter
**Coverage: FULL (planned) — integration**
- **Integration:** `003.3-INT-001` (P1)
  - **Given:** YAML with `trace_payload_async: true`, capture on, 100-span workflow
  - **When:** Workflow runs and engine closes
  - **Then:** All 100 spans appear in `*.llm.jsonl`; written by AsyncFileExporter (assert via type or instrumentation)

#### AC-8 — `trace_payload_compress: gzip` setting accepted
**Coverage: FULL (planned) — unit**
- **Unit:** `003.3-UNIT-030` (P1)
  - **Given:** Settings parser fed `trace_payload_compress: gzip`
  - **When:** Parsed
  - **Then:** Codec configured; resolved exporter path is `*.llm.jsonl.gz`
  - **AND:** Other values (e.g., `zstd`) accepted as opaque label or rejected with reserved-codec error (clarify in implementation)

#### AC-9 — Output extension `.llm.jsonl.gz`; writes via `gzip.open(..., 'at')`
**Coverage: FULL (planned) — unit**
- **Unit:** `003.3-UNIT-030` (P1)
  - **Given:** Capture on + gzip + 10 spans
  - **When:** Workflow runs
  - **Then:** File path ends in `.llm.jsonl.gz`; readable via stdlib `gunzip`; all 10 records present

#### AC-10 — Gzip writes flush at batch boundaries (mandatory)
**Coverage: FULL (planned) — unit**
- **Unit:** `003.3-UNIT-031` (P0)
  - **Given:** Gzip exporter mid-batch; raise mid-batch
  - **When:** Crash interrupts current batch
  - **Then:** Preceding batches readable by `gunzip`; current batch may be lost (documented behavior)

#### AC-11 — Sync gzip works (without async); document latency
**Coverage: FULL (planned) — unit**
- **Unit:** `003.3-UNIT-032` (P2)
  - **Given:** `trace_payload_compress: gzip` + `trace_payload_async: false`
  - **When:** Spans flow per-span
  - **Then:** Per-span open/append/close cycle works; output readable; documented as slow

#### AC-12 — `tea trace cat <file.jsonl.gz>` decompresses
**Coverage: FULL (planned) — integration**
- **Integration:** `003.3-INT-030` (P2)
  - **Given:** `*.llm.jsonl.gz` file
  - **When:** `tea trace cat foo.llm.jsonl.gz` invoked
  - **Then:** Decompressed, one JSON object per line on stdout
- **Integration:** `003.3-INT-031` (P3) — uncompressed transparent dispatch

#### AC-13 — Cleanup treats `.gz` identically to `.jsonl`
**Coverage: FULL (planned) — integration**
- **Integration:** `003.3-INT-040` (P1)
  - **Given:** Mixed directory with `.llm.jsonl` and `.llm.jsonl.gz` aged files
  - **When:** `tea trace cleanup --older-than 1` runs
  - **Then:** Both formats retired

#### AC-14 — Default off byte-identical to Story 1
**Coverage: FULL (planned) — integration**
- **Integration:** `003.3-INT-050` (P0)
  - **Given:** Neither `trace_payload_async` nor `trace_payload_compress` set
  - **When:** Story 1 sync output baseline compared
  - **Then:** Byte-for-byte identical

#### AC-15 — Async + gzip combined → batched gzip
**Coverage: FULL (planned) — integration**
- **Integration:** `003.3-INT-020` (P1)
  - **Given:** Both flags on
  - **When:** Workflow runs
  - **Then:** Output is `.jsonl.gz`, written via batch flusher; no rotation

#### AC-16 — Background-thread failure does NOT crash workflow
**Coverage: FULL (planned) — unit**
- **Unit:** `003.3-UNIT-040` (P1)
  - **Given:** Worker thread; mocked `OSError("disk full")` from disk
  - **When:** Failure occurs
  - **Then:** ERROR log; workflow continues; subsequent `export()` accumulates on queue; eventually triggers overflow policy
- **Unit:** `003.3-UNIT-041` (P2) — permission revoked mid-run; recoverable on next flush

#### AC-17 — Test coverage enumerated
**Coverage: FULL (planned, transitive)** — satisfied by union of AC-1..16 scenarios

#### AC-18 — Benchmark: 5x async speedup on slow-disk fixture
**Coverage: FULL (planned) — integration**
- **Integration:** `003.3-INT-060` (P1)
  - **Given:** 100-LLM-call workflow + 50ms-per-write slow-disk fixture
  - **When:** Sync vs async timed
  - **Then:** Async ≥ 5× faster; speedup written to test output (or surfaced via pytest-benchmark)
- **Integration:** `003.3-INT-061` (P2) — benchmark output captured into observability docs latency table

#### AC-19 — Docs include async/gzip trade-offs and example
**Coverage: FULL (planned) — doc smoke**
- **Unit (doc):** `003.3-UNIT-050` (P3)
  - **Given:** `docs/python/observability.md`
  - **When:** Substring scan
  - **Then:** Contains "async", "gzip", "data loss on hard crash", AC-19 trade-offs

---

## NFR-Derived AC Coverage (NFR-AC-1..12)

These ACs were added by the NFR review and are not yet codified into the child stories. The test design (78 scenarios) covers all of them; the coverage gap here is *contract codification* (the ACs aren't yet in the story files) rather than test scenario coverage.

| NFR-AC | Story | Coverage | Test ID(s) |
|---|---|---|---|
| **NFR-AC-1** Mandatory retention enforcement (engine init fails in production when capture on + retention unset) | 003.1+003.2 | FULL (planned) | `003.1-INT-090` (P0) — production mode fail; `003.1-INT-091` (P1) — dev warn-only |
| **NFR-AC-2** Standalone `*`/`**` rejected; match list logged at engine init | 003.1 | FULL (planned) | `003.1-UNIT-052` (P0); `003.1-UNIT-053` (P0); `003.1-UNIT-054` (P1) |
| **NFR-AC-3** `# WARNING: contains PII` sentinel as first line of `*.llm.jsonl` | 003.1 | FULL (planned) | `003.1-UNIT-070` (P0); `003.1-INT-060` (P1); `003.1-UNIT-071` (P2) |
| **NFR-AC-4** Size cap rotation + per-record truncation with marker | 003.1 | FULL (planned) | `003.1-UNIT-080` (P1); `003.1-INT-070` (P1); `003.1-UNIT-081` (P2) |
| **NFR-AC-5** Filter inside exporter; zero filesystem ops for non-`llm.call` spans | 003.1 | FULL (planned) | `003.1-UNIT-060` (P0); `003.1-INT-050` (P1) |
| **NFR-AC-6** Default-off fuzz matrix `[None, False, [], 0, "", "False", "0"]` | 003.1 | FULL (planned) | `003.1-UNIT-004` (P0); `003.1-INT-011` (P0) |
| **NFR-AC-7** Mid-run flag toggle on→off does not allow continued writes | 003.1 | FULL (planned) | `003.1-INT-080` (P1) |
| **NFR-AC-8** Cleanup rejects symlinks; preserves spans file; supports `--older-than 0` + `--dry-run`; skips in-flight | 003.2 | FULL (planned) | `003.2-UNIT-011` (P0); `003.2-UNIT-013` (P0); `003.2-UNIT-014` (P0); `003.2-UNIT-015` (P1); `003.2-UNIT-016` (P1); `003.2-UNIT-017` (P2) |
| **NFR-AC-9** Async drain on graceful shutdown; SIGKILL lossy; queue overflow drops with metric | 003.3 | FULL (planned) | `003.3-INT-010` (P0); `003.3-INT-070` (P2); `003.3-UNIT-020` (P1); `003.3-UNIT-022` (P2) |
| **NFR-AC-10** Documented per-storage-backend latency table; slow-disk fixture asserts budget | 003.1+003.3 | FULL (planned) | `003.3-INT-060` (P1); `003.3-INT-061` (P2); `003.1-INT-050` (P1) |
| **NFR-AC-11** Inline comment + sentinel test enumerating captured payload fields | 003.1 | FULL (planned) | `003.1-UNIT-010` (P0) |
| **NFR-AC-12** Observability doc presence smoke test | All | FULL (planned) | `003.1-UNIT-090` (P3); `003.2-UNIT-030` (P3); `003.3-UNIT-050` (P3) |

---

## Epic-Level Definition-of-Done Coverage

| DoD Item | Coverage | Test ID(s) |
|---|---|---|
| All 3 child stories closed | Process-only — not test-coverable | — |
| `pytest python/tests/` green | CI gate — covered by all P0/P1 tests passing | (all P0/P1) |
| Default behavior verifiably unchanged (smoke: workflow without flag → no `*.llm.jsonl`) | FULL (planned) | `003.0-E2E-001` (P0); `003.1-INT-010` (P0) |
| Enabling `extract_batch_*` produces a captured file with documented fields | FULL (planned) | `003.0-E2E-002` (P1); `003.1-INT-031` (P0) |
| Retention helper documented and tested | FULL (planned) | `003.2-E2E-001` (P0); `003.2-UNIT-030` (P3) |
| PII / retention / size guidance present in `docs/python/observability.md` | **PARTIAL (planned)** | `003.1-UNIT-090`, `003.2-UNIT-030`, `003.3-UNIT-050` (all P3 doc smoke) |

**Why partial:** The doc smoke tests verify presence of substrings ("PII", "retention", "size cap") but do not validate content accuracy, structural placement (warning at top of section), or coverage of the full guidance set required by the epic. See Recommendation #1.

---

## Cross-Cutting / Epic-Level Scenarios

These scenarios are not tied to a single child-story AC but cover gate-blocking risks and NFRs at the epic boundary:

| ID | Priority | Coverage Target |
|---|---|---|
| `003.0-E2E-001` | P0 | Default-off invariant via real CLI (no flags, no env) — **the PII no-leak signature test** |
| `003.0-E2E-002` | P1 | Full happy path — `tea run --trace-file run.jsonl --trace-llm-payloads-for extract_batch_*` |
| `003.0-E2E-003` | P1 | Story 1 + 2 + 3 stack — capture + async + gzip + retention end-to-end |
| `003.0-INT-001` | P0 | Engine startup logs resolved capture-on configuration (security audit) |
| `003.0-INT-002` | P1 | Disk-full simulation — workflow continues, ERROR logged, spans file unaffected |
| `003.0-INT-003` | P3 | Rust runtime parity — `cargo test` still passes; no Python-only references |

---

## Risk Coverage Map (rolled up from test design §6)

| Risk ID | Score | Covered? | Mitigating Test Count |
|---|---|---|---|
| SEC-001 — PII leakage | 9 (critical) | ✅ | 8 |
| DATA-001 — Retention TTL not enforced | 6 (high) | ✅ | 4 |
| OPS-001 — Disk fill | 6 (high) | ✅ | 4 |
| SEC-002 — Permissive glob | 6 (high) | ✅ | 5 |
| TECH-001 — Sync I/O latency | 6 (high) | ✅ | 5 |
| TECH-002 — Pipeline ordering | 4 (med) | ✅ | 3 |
| OPS-002 — Cleanup deletes wrong files | 4 (med) | ✅ | 5 |
| DATA-002 — Async drop on crash | 4 (med) | ✅ | 4 |
| TECH-003 — Gzip mid-record corruption | 4 (med) | ✅ | 1 (mandatory P0) |
| OPS-003 — Operator confusion `.jsonl` vs `.llm.jsonl` | 2 (low) | ✅ | 3 |
| TECH-004 — Settings env-expansion conflict | 2 (low) | ✅ | 2 |
| BUS-001 — Adoption blocked by docs | 2 (low) | ✅ | 3 |
| OPS-004 — Cleanup observability | 2 (low) | ✅ | 2 |
| TECH-005 — Rust parity drift | 1 (low) | ✅ | 1 |
| **Total** | **14 risks** | **14 / 14** | **0 gaps** |

---

## Critical Gaps

**Within ACs:** None of "no coverage" severity. Two `partial`s noted (one in Story 003.1's AC-18, one in Epic DoD's docs guidance).

**Outside the AC set (process / contract gaps):**

1. **Story-status gap (process — high severity, expected for Draft):** All 78 planned scenarios from the test design are designed but not implemented. Until tests land in `python/tests/`, this trace establishes intent only. Re-run trace after each child story implementation lands.

2. **NFR-AC codification gap (contract — high severity):** The 12 NFR-AC additions (NFR-AC-1..12) raised by the NFR review are NOT yet codified into the child story AC lists. The test design references them and provides scenarios, but without the ACs in the story files, the tests have no contract to assert against. **Most critical:** NFR-AC-1 (mandatory retention enforcement) — currently Story 003.2's AC-2 only *warns* when retention is unset; the risk profile and NFR review require escalation to engine-init *failure* in production. **Severity: high** — this is the foot-gun the epic was created to prevent.

3. **Story release-binding gap (epic-level — high severity):** Epic DoD says "All 3 child stories closed" but doesn't gate Story 003.1 from shipping without Story 003.2. Risk profile flags this as the SEC-001 deciding factor: "shipping Story 1 alone (capture without retention enforcement) is the foot-gun the epic was created to prevent." **Recommendation:** Add an explicit DoD line: "Stories 003.1 and 003.2 must release together (atomic merge or feature-flag-gated until 003.2 lands)."

4. **Doc-structure stability (Story 003.1 AC-18 partial):** No automated check that the PII warning stays at the top of the observability section. **Severity: low.**

5. **AC-3 merge-vs-override semantics (Story 003.1):** Story text is ambiguous on whether YAML list + CLI patterns merge or CLI replaces. INT-004 assumes merge — confirm with PO before implementation. **Severity: medium.** (Inherited from child trace.)

6. **`run_id` shape uniqueness for fallback path (Story 003.1 AC-9):** No scenario asserts the fallback filename is uniquely scoped per concurrent run in the same CWD. **Severity: low.** (Inherited from child trace; recommend adding `INT-024`.)

7. **Redaction hook is not in any AC:** `trace_payload_redact: [pattern,...]` was raised in NFR review as an out-of-scope-for-v1 follow-up. Epic does not log it as a deferred item. **Severity: low (process)** — recommend adding to "Out of Scope" with a forward link.

---

## Recommendations

### Must-do before story implementation begins

1. **Codify NFR-AC-1..12 into the corresponding child stories.** The test design is complete; without the ACs in the story files, scenarios `003.1-INT-090` (mandatory retention enforcement), `003.1-UNIT-052..054` (glob safety), `003.1-UNIT-070` (PII header), `003.1-UNIT-080`/`003.1-INT-070` (size cap), etc., have no contractual basis. Estimated effort: 30–60 minutes per story (12 ACs total to distribute).

2. **Add explicit Story-1-and-2 release-binding line** to the Epic Definition of Done:
   > Stories TEA-OBS-003.1 and TEA-OBS-003.2 must release together (atomic merge, single feature-flag gate, or 003.1 disabled-by-default until 003.2 lands).

3. **Pin AC-3 merge-vs-override semantics in Story 003.1 text.** Either "CLI patterns merge with YAML list, deduplicated" (current `INT-004` assumption) or "CLI patterns replace YAML list when present." Pick one, write it into the AC text.

### Should-do during implementation

4. **Add `INT-024` scenario** to Story 003.1 — fallback path uniqueness under concurrent runs in the same CWD.

5. **Promote epic-level doc smoke tests** from substring presence (`P3`) to a structured check that verifies the PII warning is in the **first 20 lines** of the observability section — addresses Story 003.1 AC-18 partial coverage.

6. **Add a `Future / Out of Scope` line** for the redaction hook (`trace_payload_redact`) so it doesn't get lost: "Reserved for follow-up epic — see TEA-OBS-003 NFR review §1.12."

### Process

7. **Re-run this trace** after each child story PR is opened — replace `(planned)` with `(implemented)` after verifying file/function names against the merged test suite. Update the partial counts and risk-coverage table as scenarios land.

8. **Add Given-When-Then comments at the top of each test function** (project's de-facto pattern in `test_observability_core.py`) so the trace stays machine-verifiable when re-run post-implementation.

---

## Risk Assessment (Coverage View)

| Coverage State | Count | Risk Posture |
|---|---|---|
| Full (planned) | 70 ACs | Low (assuming planned scenarios are implemented as designed) |
| Partial (planned) | 2 ACs | Low — gaps are doc-structure stability and partial DoD docs check |
| None | 0 ACs | — |

**Composite risk while epic is Draft:**

- **Coverage design completeness:** ✅ — 78 scenarios, all 14 risks covered, all 12 NFR-ACs covered.
- **Contract codification:** ⚠️ — 12 NFR-ACs not yet in child story AC lists; release-binding not in epic DoD. **Medium risk** — fixable with documentation edits before implementation begins.
- **Implementation realization:** Cannot be assessed until first PR lands. **Medium-high risk** by default for a Draft epic with this much surface area; the test-design + risk-profile + NFR triple coverage is the strongest risk reducer available pre-implementation.

---

## Gate YAML Block (paste-ready)

```yaml
trace:
  totals:
    requirements: 72
    full: 70
    partial: 2
    none: 0
  planning_ref: 'docs/qa/assessments/TEA-OBS-003-test-design-20260501.md'
  child_trace_ref: 'docs/qa/assessments/TEA-OBS-003.1-trace-20260501.md'
  uncovered: []
  partial:
    - ac: '003.1-AC-18'
      reason: 'Doc verification covers PII-warning content but not its top-of-section structural placement'
    - ac: 'Epic-DoD: docs guidance presence'
      reason: 'Substring smoke tests verify keywords but do not validate guidance accuracy or structural placement'
  status_caveat: 'Epic and all 3 child stories are Draft; coverage reflects 78 planned scenarios from test design rolled up across stories, not merged production tests. NFR-AC-1..12 are present in the test design but not yet codified into child story AC lists. Re-run trace after implementation to validate against actual test files.'
  contract_gaps:
    - 'NFR-AC-1..12 not yet in child story AC lists (especially mandatory retention enforcement — NFR-AC-1)'
    - 'Story 003.1 + 003.2 release-binding not in epic DoD'
    - 'AC-3 merge-vs-override semantics ambiguous in Story 003.1'
    - 'Redaction hook (trace_payload_redact) not logged as deferred follow-up'
  risk_coverage:
    SEC-001: covered_8x
    DATA-001: covered_4x
    OPS-001: covered_4x
    SEC-002: covered_5x
    TECH-001: covered_5x
    TECH-002: covered_3x
    OPS-002: covered_5x
    DATA-002: covered_4x
    TECH-003: covered_1x_p0_mandatory
    OPS-003: covered_3x
    TECH-004: covered_2x
    BUS-001: covered_3x
    OPS-004: covered_2x
    TECH-005: covered_1x
  notes: 'See docs/qa/assessments/TEA-OBS-003-trace-20260501.md'
```

---

## Story Hook Line

```text
Trace matrix: docs/qa/assessments/TEA-OBS-003-trace-20260501.md
```
