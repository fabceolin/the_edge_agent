name: file-exporter-with-path
state_schema:
  value: int
  result: str

settings:
  auto_trace: true
  trace_exporter: file
  trace_file: yaml.jsonl

nodes:
  - name: process
    run: |
      return {"result": "ok"}

edges:
  - from: __start__
    to: process
  - from: process
    to: __end__
