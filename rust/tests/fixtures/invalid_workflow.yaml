name: invalid-workflow
description: A workflow with validation errors

nodes:
  - name: node_a
    run: |
      return { result = "a" }
    # TEA-YAML-002: goto to non-existent node should fail validation
    goto: non_existent_node

  - name: node_b
    run: |
      return { result = "b" }
